CREATE TABLE IF NOT EXISTS stats (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    app VARCHAR(255),
    ip VARCHAR(255),
    uri VARCHAR(255),
    date_time_request TIMESTAMP WITHOUT TIME ZONE
);

